# Уязвимости ОС Debian

# CVE-2023-2953 в libldap-2.5-0
# Статус: Исправление недоступно в Debian 12 (Bookworm) по состоянию на окт. 2025.
# Риск: Низкий, LDAP не используется приложением.
# Переоценка: дек. 2025
CVE-2023-2953

# CVE-2025-6020 в libpam-modules
# Статус: Исправление недоступно в Debian 12 (Bookworm) по состоянию на окт. 2025.
# Риск: Низкий, PAM не используется напрямую для аутентификации в приложении.
# Переоценка: дек. 2025
CVE-2025-6020

# CVE-2025-7458 в libsqlite3-0
# Статус: Исправление недоступно в Debian 12 (Bookworm) по состоянию на окт. 2025.
# Риск: Низкий, приложение использует PostgreSQL, а не SQLite.
# Переоценка: дек. 2025
CVE-2025-7458

# CVE-2023-45853 в zlib1g
# Статус: Отмечено как 'will_not_fix' (не будет исправляться) сопровождающими Debian для этого выпуска.
# Риск: Низкий для нашего сценария использования (связано со специфическими zip-функциями).
CVE-2023-45853
